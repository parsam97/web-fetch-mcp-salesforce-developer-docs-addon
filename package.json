{
  "name": "web-fetch-mcp-salesforce-atlas-addon",
  "version": "0.1.0",
  "description": "Salesforce Atlas docs fetch handler for web-fetch-mcp (loaded via FETCH_PLUGINS).",
  "type": "module",
  "main": "dist/index.js",
  "exports": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/turndown": "^5.0.5",
    "typescript": "^5.6.0",
    "vitest": "^4.1.2"
  }
}
